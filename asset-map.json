{
	"/tmp/sambaprivate/voxels": {
		"cmd": "tools/vox2json.js $SRC > static/voxels/$BASENAME.json"
	},
	"/srv/sambaprivate/imgs": {
		"cmd": "cp $SRC > static/imgs/$NAME"
	},
	"/srv/sambaprivate/sounds": {
		"cmd": "cp $SRC > static/sounds/$NAME"
	},
	"/srv/sambaprivate/animations": {
		"cmd": "cp $SRC > static/animations/$NAME"
	},
	"/tmp/sambaprivate/meshes": {
		"cmd": "cp $SRC > static/meshes/$NAME"
	}
}
